# 复制为同目录下的 application-local.yml（已在 .gitignore），启动时加：--spring.profiles.active=local
# 密钥建议全部用环境变量，勿提交本文件：
#   CS_LLM_API_KEY、COS_SECRET_ID、COS_SECRET_KEY
cs:
  llm:
    enabled: true
    model: qwen3.5-plus

# app.cos.secret-id / secret-key 也可在 application-local.yml 中覆盖（勿提交）
